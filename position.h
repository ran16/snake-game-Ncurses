#ifndef POSITION
#define POSITION

struct position{
	int x; // coordinates on x-axis(columns)
	int y; //coordinates on y-axis(rows)

	//default constructor
	position();
	//second constructor
	position(int ny, int nx);
};


#endif
